
{
  "name": "droidnet-sentinel",
  "version": "1.0.0",
  "description": "Android CTF Network Monitor",
  "main": "index.html",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html index.tsx types.ts metadata.json capacitor.config.json dist/ && cp -rv utils services dist/ || true"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "lucide-react": "^0.563.0",
    "@google/genai": "^1.39.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}

